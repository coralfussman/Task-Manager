<div class="container-forms">

<%= form_for @project do |f| %>

<%= f.label :name, 'Project Name:' %>
<%= f.text_field :name %>

<%= f.label :team_id, 'Team Name:' %>
<%= f.collection_select :team_id, Team.all, :id, :name %>

<%= f.label :description %>
<%= f.text_field :description %>

<%#= f.fields_for :member_id, @member.name do |member_form| %>
<%#= member_form.label :member_id %>
<%#= member_form.checkbox :member_id %>

<%= f.label :date, 'Date Due:' %>
<%= f.date_field :date %>

<%= f.submit %>
<%# end %>
<% end %>
</div>